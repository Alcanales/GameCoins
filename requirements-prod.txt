# requirements-prod.txt
# Dependencias para PRODUCCIÓN (Render, Heroku, etc.)

# Incluir dependencias base
-r requirements-base.txt

# Monitoring y logging (solo producción)
sentry-sdk[fastapi]==1.39.2
python-json-logger==2.0.7

# Rate limiting
slowapi==0.1.9

# Opcional: Cache con Redis
redis==5.0.1
hiredis==2.3.2
